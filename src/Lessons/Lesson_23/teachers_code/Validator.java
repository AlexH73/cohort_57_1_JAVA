package Lessons.Lesson_23.teachers_code;

public class Validator {
    protected boolean validate() {
        // выполняет сложную логику проверки

        return ((int) (Math.random() * 10)) % 2 == 0;
    }
}
