package Practice.cycles;

/**
 * <h1>✏️ Условие:</h1>
 * Напиши программу, которая выводит таблицу умножения для чисел от 1 до 10.
 * <p>
 * <b>💡 Подсказка:</b>
 * <p>
 * Используй вложенные циклы:
 * <p>
 * Внешний цикл для чисел от 1 до 10 (i)
 * <p>
 * Внутренний цикл для чисел от 1 до 10 (j)
 * <p>
 * Формат вывода: i * j = результат
 * <p>
 * Название класса: MultiplicationTable
 * <p>
 * Переменные:
 * <p>
 * i, j — индексы циклов
 * <p>
 * result — результат умножения (можно обойтись и без неё, просто выводить i * j)
 */
public class MultiplicationTable {
    public static void main(String[] args) {
        for (int i = 1; i <= 10; i++) {
            for (int j = 1; j <= 10; j++) {
                System.out.printf("%4d * %2d = %2d", i, j, i * j);
            }
            System.out.println();
        }
    }
    /*
    Пояснение:
    Внешний цикл i от 1 до 10 — строки таблицы

    Внутренний цикл j от 1 до 10 — столбцы

    На каждой итерации выводится произведение: i * j = результат

    После каждого блока (одного числа i) добавляется пустая строка для читабельности

    %4d — значит "целое число, шириной 4 символа".
    Это даёт одинаковое расстояние между числами, даже если они разной длины.

    printf() не добавляет автоматический перевод строки,
    поэтому System.out.println() используется после внутреннего цикла.
    */
}
